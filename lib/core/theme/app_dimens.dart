import 'package:flutter/material.dart';

class AppDimens {
  static const double spaceXS = 8.0;
  static const double spaceS = 12.0;
  static const double spaceM = 16.0;
  static const double spaceL = 24.0;
  static const double spaceXL = 32.0;

  static const double radiusS = 8.0;   // Для мелких тегов/чипсов
  static const double radiusM = 14.0;  // Основной радиус для карточек и кнопок
  static const double radiusL = 24.0;  // Для больших кастомных шторок (Bottom Sheet)
  static const double radiusRound = 99.0;

  static const double avatarSize = 48.0;
  static const double iconSize = 24.0;

  static const EdgeInsets screenPadding = EdgeInsets.all(spaceM);
}